/**
 * 
 */
function JobDescription(){
    this.owner = "";
    this.created = "";
    this.components = [];
    this.schedule = "";
};

JobDescription.prototype.populate = function(obj){
    this.owner = (obj.owner) ? obj.owner : "";
    this.created = (obj.created) ? obj.created : "";
    this.components = (obj.components) ? obj.components : [];
    this.schedule = (obj.schedule) ? obj.schedule : "";
    if(obj._id)
        this._id = obj._id;
};

module.exports = JobDescription;